import Link from "next/link";
import { Activity, ExternalLink, Mail, MapPin, Phone, ShieldCheck, Sparkles, UserRound } from "lucide-react";
import { notFound } from "next/navigation";
import { requireAdmin } from "../../../lib/admin";
import { db } from "../../../lib/db";
import { setBusinessVerificationAdminAction } from "../../../actions/admin-verification-control";
export default async function AdminBusinessPage({params,searchParams}:{params:Promise<{id:string}>;searchParams?:Promise<{verification?:string}>}){await requireAdmin();const{id}=await params;const query=await searchParams;const business=await db.business.findFirst({where:{id,deletedAt:null},select:{id:true,name:true,nameEn:true,slug:true,businessType:true,businessCategory:true,description:true,city:true,district:true,address:true,phone:true,whatsapp:true,email:true,website:true,isPublished:true,isVerified:true,onboardingCompleted:true,onboardingStep:true,createdAt:true,updatedAt:true,publishedAt:true,owner:{select:{id:true,name:true,email:true,createdAt:true}},plan:{select:{code:true,name:true,monthlyPrice:true}},_count:{select:{products:true,services:true,customers:true,orders:true,bookings:true,branches:true,contactPersons:true,galleryItems:true}}}});if(!business)notFound();const[recentOrders,recentBookings]=await Promise.all([db.order.findMany({where:{businessId:business.id},orderBy:{createdAt:"desc"},take:8,select:{id:true,status:true,orderType:true,total:true,createdAt:true,customer:{select:{name:true,phone:true}}}}),db.booking.findMany({where:{businessId:business.id},orderBy:{createdAt:"desc"},take:8,select:{id:true,status:true,bookingDate:true,bookingTime:true,createdAt:true,customer:{select:{name:true,phone:true}},service:{select:{name:true}}}})]);const date=(value:Date|null)=>value?new Intl.DateTimeFormat("ar-SA",{dateStyle:"medium",timeStyle:"short",timeZone:"Asia/Riyadh"}).format(value):"—";
return <main dir="rtl" className="min-h-screen bg-[#f7faf9] px-4 py-7 text-[#10292d] sm:px-6"><div className="mx-auto max-w-6xl space-y-5">{query?.verification?<div role="status" className="rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-800">{query.verification==="verified"?"تم توثيق صفحة المنشأة من الإدارة.":query.verification==="unverified"?"تم إلغاء توثيق صفحة المنشأة من الإدارة.":"حالة التوثيق مطابقة بالفعل."}</div>:null}<header className="relative overflow-hidden rounded-[30px] bg-[#07181b] p-6 text-white sm:p-7"><div className="absolute -left-16 -top-20 h-56 w-56 rounded-full bg-[#00e5a8]/10 blur-3xl"/><div className="relative"><div className="flex flex-wrap items-start justify-between gap-5"><div><Link href="/admin" className="text-[9px] font-black tracking-[.16em] text-[#5cebd7]" dir="ltr">INFRO BUSINESS INTELLIGENCE</Link><h1 className="mt-3 text-2xl font-black sm:text-3xl">{business.name}</h1><p className="mt-1 text-sm text-slate-300" dir="ltr">ir.sa/{business.slug}</p><div className="mt-4 flex flex-wrap gap-2"><Status active={business.isPublished} on="منشورة" off="غير منشورة"/><Status active={business.isVerified} on="موثقة" off="غير موثقة"/><Status active={business.onboardingCompleted} on="الإعداد مكتمل" off={`الإعداد: ${business.onboardingStep??"غير مكتمل"}`}/><span className="rounded-full border border-white/10 bg-white/[.07] px-3 py-1 text-[10px] font-black text-[#5cebd7]">{business.plan?.name??"Free"}</span></div></div><a href={`/${business.slug}`} target="_blank" rel="noreferrer" className="inline-flex h-11 items-center gap-2 rounded-xl bg-[#35e4cb] px-4 text-xs font-black text-[#07181b]">فتح الصفحة العامة<ExternalLink className="h-4 w-4"/></a></div><div className="mt-6 grid grid-cols-2 gap-2 sm:grid-cols-4"><MetricDark title="المنتجات" value={business._count.products}/><MetricDark title="الخدمات" value={business._count.services}/><MetricDark title="العملاء" value={business._count.customers}/><MetricDark title="الطلبات + الحجوزات" value={business._count.orders+business._count.bookings}/></div></div></header>
<section className="grid gap-4 lg:grid-cols-[1.2fr_.8fr]"><article className="rounded-[26px] border border-slate-200 bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><span className="text-[9px] font-black tracking-[.14em] text-[#008f87]" dir="ltr">TRUST CONTROL</span><div className="mt-2 flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-[#008f87]"/><h2 className="font-black text-slate-950">توثيق صفحة المنشأة</h2></div><p className="mt-2 max-w-2xl text-xs leading-6 text-slate-500">إجراء إداري مستقل ومسجل تدقيقيًا. لا تدخل الإدارة جلسة العميل ولا تعدل محتوى صفحته باسمه.</p></div><form action={setBusinessVerificationAdminAction}><input type="hidden" name="businessId" value={business.id}/><input type="hidden" name="verified" value={business.isVerified?"false":"true"}/><button className={`min-h-11 rounded-xl px-4 text-xs font-black ${business.isVerified?"border border-rose-200 bg-rose-50 text-rose-700":"bg-[#07181b] text-white"}`}>{business.isVerified?"إلغاء توثيق الصفحة":"توثيق الصفحة الآن"}</button></form></div></article><article className="rounded-[26px] border border-slate-200 bg-[linear-gradient(135deg,#effbf9,#fff)] p-5"><span className="text-[9px] font-black tracking-[.14em] text-[#008f87]" dir="ltr">BUSINESS SIGNAL</span><div className="mt-3 flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-white text-[#008f87] shadow-sm"><Activity className="h-5 w-5"/></span><div><b className="block text-sm text-slate-950">{business.onboardingCompleted?"جاهزية الإعداد مكتملة":"الإعداد يحتاج متابعة"}</b><span className="mt-1 block text-[10px] text-slate-500">آخر تحديث {date(business.updatedAt)}</span></div></div></article></section>
<section className="grid gap-4 lg:grid-cols-2"><InfoCard icon={<UserRound className="h-4 w-4"/>} eyebrow="OWNER PROFILE" title="بيانات المالك"><Row label="الاسم" value={business.owner.name}/><Row label="البريد" value={business.owner.email}/><Row label="تاريخ الحساب" value={date(business.owner.createdAt)}/><Row label="معرّف المستخدم" value={business.owner.id} mono/></InfoCard><InfoCard icon={<Sparkles className="h-4 w-4"/>} eyebrow="BUSINESS PROFILE" title="بيانات المنشأة"><Row label="التصنيف" value={business.businessCategory??business.businessType}/><Row label="الخطة" value={`${business.plan?.name??"Free"}${business.plan?` · ${business.plan.monthlyPrice} ر.س`:""}`}/><Row label="أُنشئت" value={date(business.createdAt)}/><Row label="آخر تحديث" value={date(business.updatedAt)}/><Row label="آخر نشر" value={date(business.publishedAt)}/></InfoCard></section>
<section className="rounded-[26px] border border-slate-200 bg-white p-5"><span className="text-[9px] font-black tracking-[.14em] text-[#008f87]" dir="ltr">REACHABILITY</span><h2 className="mt-1 font-black text-slate-950">التواصل والموقع</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Contact icon={<Mail className="h-4 w-4"/>} label="البريد" value={business.email}/><Contact icon={<Phone className="h-4 w-4"/>} label="الهاتف" value={business.phone}/><Contact icon={<Phone className="h-4 w-4"/>} label="واتساب" value={business.whatsapp}/><Contact icon={<MapPin className="h-4 w-4"/>} label="الموقع" value={[business.city,business.district,business.address].filter(Boolean).join(" · ")||null}/></div>{business.description?<p className="mt-4 rounded-2xl bg-[#f7faf9] p-4 text-sm leading-7 text-slate-600">{business.description}</p>:null}</section>
<section className="grid gap-4 lg:grid-cols-2"><ActivityCard eyebrow="RECENT ORDERS" title="آخر الطلبات">{recentOrders.length?recentOrders.map(order=><div key={order.id} className="rounded-2xl border border-slate-100 bg-[#fbfdfd] p-3 text-xs"><div className="flex items-center justify-between gap-3"><b>{order.customer.name}</b><Status active={order.status==="confirmed"||order.status==="completed"} on={order.status} off={order.status}/></div><span className="mt-1 block text-slate-500">{order.customer.phone} · {order.orderType} · {order.total} ر.س</span><span className="mt-1 block text-slate-400">{date(order.createdAt)}</span></div>):<Empty text="لا توجد طلبات."/>}</ActivityCard><ActivityCard eyebrow="RECENT BOOKINGS" title="آخر الحجوزات">{recentBookings.length?recentBookings.map(booking=><div key={booking.id} className="rounded-2xl border border-slate-100 bg-[#fbfdfd] p-3 text-xs"><div className="flex items-center justify-between gap-3"><b>{booking.customer.name}</b><Status active={booking.status==="confirmed"||booking.status==="completed"} on={booking.status} off={booking.status}/></div><span className="mt-1 block text-slate-500">{booking.service?.name??"خدمة"} · {booking.bookingDate} {booking.bookingTime}</span><span className="mt-1 block text-slate-400">{booking.customer.phone}</span></div>):<Empty text="لا توجد حجوزات."/>}</ActivityCard></section>
<div className="flex gap-2 rounded-2xl border border-[#bfe9e3] bg-[#effbf9] p-4 text-xs leading-6 text-[#075d58]"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0"/><p><b>إدارة آمنة:</b> التوثيق منفصل عن جلسة العميل، وكل تغيير إداري يبقى قابلًا للتدقيق.</p></div></div></main>}
function MetricDark({title,value}:{title:string;value:number}){return <article className="rounded-2xl border border-white/10 bg-white/[.06] p-3"><span className="text-[9px] text-slate-400">{title}</span><b className="mt-1 block text-xl font-black text-white">{new Intl.NumberFormat("ar-SA").format(value)}</b></article>}
function InfoCard({icon,eyebrow,title,children}:{icon:React.ReactNode;eyebrow:string;title:string;children:React.ReactNode}){return <article className="rounded-[26px] border border-slate-200 bg-white p-5"><span className="text-[9px] font-black tracking-[.14em] text-[#008f87]" dir="ltr">{eyebrow}</span><div className="mt-2 flex items-center gap-2 text-slate-950"><span className="text-[#008f87]">{icon}</span><h2 className="font-black">{title}</h2></div><div className="mt-4 space-y-3 text-sm">{children}</div></article>}
function ActivityCard({eyebrow,title,children}:{eyebrow:string;title:string;children:React.ReactNode}){return <article className="rounded-[26px] border border-slate-200 bg-white p-5"><span className="text-[9px] font-black tracking-[.14em] text-[#008f87]" dir="ltr">{eyebrow}</span><h2 className="mt-1 font-black text-slate-950">{title}</h2><div className="mt-4 space-y-2">{children}</div></article>}
function Row({label,value,mono=false}:{label:string;value:string;mono?:boolean}){return <div className="flex items-start justify-between gap-4 border-b border-slate-100 pb-2 last:border-0"><span className="text-slate-400">{label}</span><span className={`text-left font-bold text-slate-700 ${mono?"font-mono text-[11px]":""}`}>{value}</span></div>}
function Contact({icon,label,value}:{icon:React.ReactNode;label:string;value:string|null}){return <div className="rounded-2xl border border-slate-100 bg-[#fbfdfd] p-3"><span className="flex items-center gap-2 text-[10px] font-bold text-slate-400"><span className="text-[#008f87]">{icon}</span>{label}</span><b className="mt-2 block break-words text-xs text-slate-700">{value||"—"}</b></div>}
function Status({active,on,off}:{active:boolean;on:string;off:string}){return <span className={`rounded-full px-2.5 py-1 text-[9px] font-black ${active?"bg-emerald-50 text-emerald-700":"bg-slate-100 text-slate-500"}`}>{active?on:off}</span>}
function Empty({text}:{text:string}){return <div className="rounded-2xl bg-[#f7faf9] px-4 py-6 text-center text-sm text-slate-500">{text}</div>}
