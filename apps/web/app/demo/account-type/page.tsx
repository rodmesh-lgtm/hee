"use client";

import { AccountTypeSelectionPrototype } from "../../../components/organization-prototype";

export default function DemoAccountTypePage() {
  return <AccountTypeSelectionPrototype onSelect={() => undefined} selected={null} />;
}
