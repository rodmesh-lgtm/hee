import { Prisma } from "@prisma/client";
import { Archive, ArrowDown, ArrowUp, BellPlus, FilePenLine, NotebookPen, Pin, PinOff, Plus, RotateCcw, Search, ShieldCheck, Trash2 } from "lucide-react";
import Link from "next/link";
import { redirect } from "next/navigation";
import { archiveBusinessNoteAction, createBusinessNoteAction, deleteBusinessNoteAction, moveBusinessNoteAction, restoreBusinessNoteAction, toggleBusinessNotePinAction, updateBusinessNoteAction } from "../../actions/business-notes";
import { getCurrentUser } from "../../lib/auth";
import { getActiveBusinessForUser } from "../../lib/active-business";
import { db } from "../../lib/db";
import { isBusinessNotesSchemaReady } from "../../lib/business-notes/schema-readiness";

type Note = { id:string; title:string; body:string; isPinned:boolean; sortOrder:number; category:string; priority:string; tags:string[]; status:string; createdAt:Date; updatedAt:Date; archivedAt:Date|null; reminderCount:bigint };
const priorityLabel:Record<string,string>={low:"منخفضة",normal:"عادية",high:"عالية",urgent:"عاجلة"};
const statusLabel:Record<string,string>={draft:"مسودة",active:"نشطة",archived:"مؤرشفة"};

function Notice({ params }: { params: Record<string, string | undefined> }) {
  const entry = Object.entries(params).find(([key,value]) => value && !["q","view","priority"].includes(key));
  if (!entry) return null;
  const success = entry[1] === "success";
  const linked = entry[1] === "linked-reminder";
  return <div role="status" className={`rounded-2xl border px-4 py-3 text-sm font-bold ${success?"border-emerald-200 bg-emerald-50 text-emerald-800":"border-amber-200 bg-amber-50 text-amber-900"}`}>{success?"تم حفظ التغيير في مذكرات أعمالك.":linked?"لا يمكن حذف هذه المذكرة نهائيًا لأنها مرتبطة بتذكير. استخدم الأرشفة للحفاظ على سلامة السجل.":"تعذر تنفيذ العملية. لم نغيّر أي مذكرة غير مؤكدة."}</div>;
}

function Pending(){return <div dir="rtl" className="space-y-6 pb-10"><header className="rounded-[28px] border border-slate-200 bg-[#07181b] p-6 text-white"><p className="text-[10px] font-black tracking-[.16em] text-[#4ee7d4]">INFRO BUSINESS PRODUCTIVITY</p><h1 className="mt-3 text-2xl font-black">مذكرات الأعمال</h1></header><section className="rounded-[24px] border border-amber-200 bg-amber-50 p-5 text-sm leading-7 text-amber-950">قاعدة بيانات هذه البيئة لم تُحدَّث بعد بالمخطط المطلوب. الواجهة متوقفة بأمان ولم يتم إنشاء أو حذف أي بيانات.</section></div>}

export default async function BusinessNotesPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){
  const user=await getCurrentUser(); if(!user)redirect("/login");
  const business=await getActiveBusinessForUser(user.id); if(!business)redirect("/dashboard?business=required");
  const params=await searchParams; if(!await isBusinessNotesSchemaReady())return <Pending/>;
  const notes=await db.$queryRaw<Note[]>(Prisma.sql`
    SELECT n."id",n."title",n."body",n."isPinned",n."sortOrder",n."category",n."priority",n."tags",n."status",n."createdAt",n."updatedAt",n."archivedAt",
      (SELECT COUNT(*)::bigint FROM "SmartReminder" r WHERE r."businessId"=n."businessId" AND r."businessNoteId"=n."id") AS "reminderCount"
    FROM "BusinessNote" n WHERE n."businessId"=${business.id}
    ORDER BY n."isPinned" DESC,n."sortOrder" ASC,n."updatedAt" DESC LIMIT 250
  `);
  const q=(params.q??"").normalize("NFKC").trim().toLocaleLowerCase("ar");
  const view=["active","draft","archived","all"].includes(params.view??"")?params.view!:"active";
  const priority=["low","normal","high","urgent","all"].includes(params.priority??"")?params.priority!:"all";
  const filtered=notes.filter(note=>(view==="all"||note.status===view)&&(priority==="all"||note.priority===priority)&&(!q||[note.title,note.body,note.category,...note.tags].some(value=>value.toLocaleLowerCase("ar").includes(q))));
  const counts={active:notes.filter(n=>n.status==="active").length,draft:notes.filter(n=>n.status==="draft").length,archived:notes.filter(n=>n.status==="archived").length,pinned:notes.filter(n=>n.status!=="archived"&&n.isPinned).length};

  return <div dir="rtl" className="space-y-6 pb-10">
    <header className="relative overflow-hidden rounded-[28px] border border-slate-200 bg-[#07181b] p-6 text-white shadow-sm sm:p-7"><div className="absolute -left-20 -top-24 h-64 w-64 rounded-full bg-[#00d8c6]/15 blur-3xl"/><div className="relative flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="mb-3 flex items-center gap-2 text-[10px] font-black tracking-[.16em] text-[#4ee7d4]"><NotebookPen className="h-4 w-4"/>INFRO BUSINESS PRODUCTIVITY</div><h1 className="text-2xl font-black sm:text-3xl">مذكرات الأعمال</h1><p className="mt-2 max-w-2xl text-sm leading-7 text-slate-300">احتفظ بالقرارات والمتابعات المهمة، صنّفها وثبّتها واربطها بتذكير دون فقد سياق العمل.</p></div><div className="flex items-center gap-2 rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-xs font-bold text-[#a8f2e8]"><ShieldCheck className="h-4 w-4"/>خاصة بالمنشأة النشطة</div></div></header>
    <Notice params={params}/>
    <section className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[["النشطة",counts.active],["المسودات",counts.draft],["المثبتة",counts.pinned],["المؤرشفة",counts.archived]].map(([label,value])=><div key={String(label)} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-[11px] font-bold text-slate-500">{label}</p><p className="mt-1 text-xl font-black text-slate-900">{String(value)}</p></div>)}</section>
    <section className="rounded-[26px] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-[#e9fbf8] text-[#009d93]"><Plus className="h-5 w-5"/></div><div><h2 className="font-black text-slate-900">مذكرة جديدة</h2><p className="mt-1 text-xs text-slate-500">يمكن حفظها نشطة أو كمسودة.</p></div></div><form action={createBusinessNoteAction} className="grid gap-3"><input name="title" required maxLength={160} placeholder="عنوان المذكرة" className="rounded-2xl border border-slate-200 px-4 py-3 text-sm font-bold outline-none focus:border-[#00bfae]"/><textarea name="body" required maxLength={8000} rows={5} placeholder="اكتب تفاصيل المذكرة..." className="resize-y rounded-2xl border border-slate-200 px-4 py-3 text-sm leading-7 outline-none focus:border-[#00bfae]"/><div className="grid gap-3 sm:grid-cols-3"><input name="category" maxLength={64} defaultValue="عام" aria-label="التصنيف" className="rounded-xl border border-slate-200 px-3 py-2 text-sm"/><select name="priority" defaultValue="normal" aria-label="الأولوية" className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="low">أولوية منخفضة</option><option value="normal">أولوية عادية</option><option value="high">أولوية عالية</option><option value="urgent">عاجلة</option></select><select name="status" defaultValue="active" aria-label="حالة المذكرة" className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="active">نشطة</option><option value="draft">مسودة</option></select></div><input name="tags" maxLength={500} placeholder="وسوم مفصولة بفواصل: عقد، عميل، متابعة" className="rounded-xl border border-slate-200 px-3 py-2 text-sm"/><div className="flex flex-wrap items-center justify-between gap-3"><label className="flex items-center gap-2 text-xs font-bold text-slate-600"><input type="checkbox" name="isPinned" className="h-4 w-4 accent-[#009d93]"/>تثبيت في الأعلى</label><button type="submit" className="rounded-xl bg-[#07181b] px-5 py-3 text-xs font-black text-white">حفظ المذكرة</button></div></form></section>
    <section className="rounded-[22px] border border-slate-200 bg-white p-3 shadow-sm"><form method="get" className="grid gap-2 sm:grid-cols-[1fr_auto_auto_auto]"><label className="relative"><Search className="absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400"/><input name="q" defaultValue={params.q??""} placeholder="ابحث في العنوان، المحتوى، التصنيف أو الوسوم" className="w-full rounded-xl border border-slate-200 py-2.5 pl-3 pr-9 text-sm"/></label><select name="view" defaultValue={view} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="active">النشطة</option><option value="draft">المسودات</option><option value="archived">المؤرشفة</option><option value="all">الكل</option></select><select name="priority" defaultValue={priority} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="all">كل الأولويات</option><option value="urgent">عاجلة</option><option value="high">عالية</option><option value="normal">عادية</option><option value="low">منخفضة</option></select><button className="rounded-xl bg-slate-900 px-4 py-2 text-xs font-black text-white">تطبيق</button></form></section>
    <section className="space-y-3"><div className="flex items-center justify-between"><h2 className="font-black text-slate-900">مذكراتك</h2><span className="rounded-full bg-slate-100 px-3 py-1 text-[10px] font-black text-slate-500">{filtered.length} نتيجة</span></div>{filtered.length===0?<div className="rounded-[24px] border border-dashed border-slate-300 bg-white p-8 text-center text-sm text-slate-500">لا توجد مذكرات مطابقة. غيّر البحث أو الفلاتر، أو أضف مذكرة جديدة.</div>:filtered.map(note=><article key={note.id} className="rounded-[24px] border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-start justify-between gap-3"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2">{note.isPinned?<Pin className="h-4 w-4 text-[#009d93]"/>:<FilePenLine className="h-4 w-4 text-slate-400"/>}<h3 className="font-black text-slate-900">{note.title}</h3><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-black">{statusLabel[note.status]??note.status}</span><span className="rounded-full bg-amber-50 px-2 py-1 text-[10px] font-black text-amber-800">{priorityLabel[note.priority]??note.priority}</span><span className="rounded-full bg-[#edfafa] px-2 py-1 text-[10px] font-black text-[#007d76]">{note.category}</span></div><div className="mt-2 flex flex-wrap gap-1">{note.tags.map(tag=><span key={tag} className="rounded-lg bg-slate-50 px-2 py-1 text-[10px] text-slate-500">#{tag}</span>)}</div></div><span className="shrink-0 text-[10px] font-bold text-slate-400">{new Intl.DateTimeFormat("ar-SA",{dateStyle:"medium"}).format(note.updatedAt)}</span></div>{note.status!=="archived"?<form action={updateBusinessNoteAction} className="space-y-3"><input type="hidden" name="noteId" value={note.id}/><input name="title" required maxLength={160} defaultValue={note.title} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm font-bold"/><textarea name="body" required maxLength={8000} defaultValue={note.body} rows={4} className="w-full resize-y rounded-xl border border-slate-200 px-3 py-2 text-sm leading-7"/><div className="grid gap-2 sm:grid-cols-3"><input name="category" maxLength={64} defaultValue={note.category} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"/><select name="priority" defaultValue={note.priority} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="low">منخفضة</option><option value="normal">عادية</option><option value="high">عالية</option><option value="urgent">عاجلة</option></select><select name="status" defaultValue={note.status} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="active">نشطة</option><option value="draft">مسودة</option></select></div><input name="tags" defaultValue={note.tags.join("، ")} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm"/><button type="submit" className="rounded-xl border border-slate-200 px-4 py-2 text-xs font-black text-slate-700">حفظ التعديل</button></form>:<p className="whitespace-pre-wrap text-sm leading-7 text-slate-600">{note.body}</p>}<div className="mt-4 flex flex-wrap gap-2 border-t border-slate-100 pt-4">{note.status!=="archived"?<><Link href={`/dashboard/reminders?note=${encodeURIComponent(note.id)}`} className="inline-flex items-center gap-1.5 rounded-xl bg-[#07181b] px-3 py-2 text-[11px] font-black text-white"><BellPlus className="h-3.5 w-3.5"/>أضف تذكيرًا</Link><form action={toggleBusinessNotePinAction}><input type="hidden" name="noteId" value={note.id}/><button className="inline-flex items-center gap-1.5 rounded-xl bg-[#e9fbf8] px-3 py-2 text-[11px] font-black text-[#007d76]">{note.isPinned?<PinOff className="h-3.5 w-3.5"/>:<Pin className="h-3.5 w-3.5"/>}{note.isPinned?"إلغاء التثبيت":"تثبيت"}</button></form><form action={moveBusinessNoteAction}><input type="hidden" name="noteId" value={note.id}/><input type="hidden" name="direction" value="up"/><button aria-label="تحريك للأعلى" className="rounded-xl border border-slate-200 p-2"><ArrowUp className="h-4 w-4"/></button></form><form action={moveBusinessNoteAction}><input type="hidden" name="noteId" value={note.id}/><input type="hidden" name="direction" value="down"/><button aria-label="تحريك للأسفل" className="rounded-xl border border-slate-200 p-2"><ArrowDown className="h-4 w-4"/></button></form><form action={archiveBusinessNoteAction}><input type="hidden" name="noteId" value={note.id}/><button className="inline-flex items-center gap-1.5 rounded-xl border border-amber-200 px-3 py-2 text-[11px] font-black text-amber-800"><Archive className="h-3.5 w-3.5"/>أرشفة</button></form></>:<form action={restoreBusinessNoteAction}><input type="hidden" name="noteId" value={note.id}/><button className="inline-flex items-center gap-1.5 rounded-xl border border-emerald-200 px-3 py-2 text-[11px] font-black text-emerald-700"><RotateCcw className="h-3.5 w-3.5"/>استعادة</button></form>}<span className="self-center text-[10px] font-bold text-slate-400">{Number(note.reminderCount)} تذكير مرتبط</span><form action={deleteBusinessNoteAction} className="mr-auto"><input type="hidden" name="noteId" value={note.id}/><button className="inline-flex items-center gap-1.5 rounded-xl border border-rose-200 px-3 py-2 text-[11px] font-black text-rose-700"><Trash2 className="h-3.5 w-3.5"/>حذف نهائي</button></form></div></article>)}</section>
  </div>;
}
