import assert from "node:assert/strict";
import { describe, it } from "node:test";
import { DEFAULT_PLATFORM_BRAND, PLATFORM_BRAND_SETTING_KEY, platformBrandCssVariables } from "../lib/platform-brand-config";
describe("INFRO platform brand defaults",()=>{it("uses approved identity",()=>{assert.equal(PLATFORM_BRAND_SETTING_KEY,"platform.brand.v1");assert.equal(DEFAULT_PLATFORM_BRAND.brandNameEn,"INFRO");assert.equal(DEFAULT_PLATFORM_BRAND.brandNameAr,"انفرو");assert.equal(DEFAULT_PLATFORM_BRAND.primaryColor,"#00d8c6");assert.equal(DEFAULT_PLATFORM_BRAND.secondaryColor,"#08bfe8");assert.equal(DEFAULT_PLATFORM_BRAND.accentColor,"#00eea8")});it("maps editable colors",()=>{const v=platformBrandCssVariables(DEFAULT_PLATFORM_BRAND);assert.equal(v["--brand-primary"],"#00d8c6");assert.equal(v["--brand-header-background"],"#020b14");assert.equal(v["--brand-footer-background"],"#020b14")});it("keeps safe SEO defaults",()=>{assert.equal(DEFAULT_PLATFORM_BRAND.robotsIndex,true);assert.equal(DEFAULT_PLATFORM_BRAND.robotsFollow,true);assert.match(DEFAULT_PLATFORM_BRAND.seoTitleEn,/INFRO/);assert.match(DEFAULT_PLATFORM_BRAND.seoTitleAr,/انفرو/)})});
