"use client";

import { BrainCircuit, CalendarDays, Mic, Square, UserRound, WandSparkles } from "lucide-react";
import { useEffect, useRef, useState } from "react";

const MAX_SECONDS = 5 * 60;
const LANGUAGE_OPTIONS = [["auto","تلقائي / Auto"],["ar","العربية"],["en","English"],["es","Español"],["ur","اردو"],["zh-CN","中文"]] as const;
type Suggestion={title:string|null;noteType:string;summary:string|null;outcome:string|null;nextAction:string|null;stakeholder:string|null;responsiblePerson:string|null;priority:string;workHealth:string;businessDueDate:string|null;reminderSuggested:boolean;reminderTitle:string|null;reminderReason:string|null};
type BrowserSpeechRecognition={lang:string;continuous:boolean;interimResults:boolean;maxAlternatives:number;start:()=>void;stop:()=>void;abort:()=>void;onresult:((event:{resultIndex:number;results:ArrayLike<{isFinal:boolean;0:{transcript:string}}>})=>void)|null;onerror:((event:{error?:string})=>void)|null;onend:(()=>void)|null};
type SpeechRecognitionConstructor=new()=>BrowserSpeechRecognition;
function browserSpeechConstructor():SpeechRecognitionConstructor|null{if(typeof window==="undefined")return null;const c=window as unknown as{SpeechRecognition?:SpeechRecognitionConstructor;webkitSpeechRecognition?:SpeechRecognitionConstructor};return c.SpeechRecognition??c.webkitSpeechRecognition??null}
function browserLanguage(h:string){if(h==="ar")return"ar-SA";if(h==="en")return"en-US";if(h==="es")return"es-ES";if(h==="ur")return"ur-PK";if(h==="zh-CN")return"zh-CN";return typeof navigator!=="undefined"&&navigator.language?navigator.language:"ar-SA"}
function recorderMimeType(){if(typeof MediaRecorder==="undefined")return"";for(const v of["audio/webm;codecs=opus","audio/mp4","audio/webm","audio/ogg;codecs=opus"])if(MediaRecorder.isTypeSupported(v))return v;return""}
function extensionFor(t:string){if(t.includes("mp4"))return"m4a";if(t.includes("ogg"))return"ogg";return"webm"}
function errorText(c:string){if(c==="rate_limited")return"تم استخدام الذكاء الاصطناعي عدة مرات خلال فترة قصيرة. حاول بعد قليل.";if(c==="voice_unavailable"||c==="ai_unavailable")return"خدمات الذكاء الاصطناعي غير مفعّلة في هذه البيئة بعد.";if(c==="unsupported_audio")return"صيغة التسجيل غير مدعومة.";if(c==="invalid_audio"||c==="file_too_large"||c==="input_too_large")return"المحتوى طويل أو حجمه أكبر من الحد المسموح.";if(c==="transcription_timeout"||c==="ai_timeout")return"استغرقت المعالجة وقتًا أطول من المتوقع. حاول مرة أخرى.";if(c==="empty_transcript")return"لم أتمكن من استخراج كلام واضح من التسجيل.";return"تعذرت المعالجة الذكية الآن. يمكنك متابعة كتابة وحفظ المذكرة يدويًا."}
function setFormValue(form:HTMLFormElement|null,name:string,value:string|null,overwrite=false){if(!form||!value)return;const f=form.elements.namedItem(name);if(!(f instanceof HTMLInputElement||f instanceof HTMLTextAreaElement||f instanceof HTMLSelectElement))return;if(!overwrite&&f.value.trim())return;f.value=value;f.dispatchEvent(new Event("input",{bubbles:true}));f.dispatchEvent(new Event("change",{bubbles:true}))}
export function BusinessNoteVoiceTextarea({voiceAvailable}:{voiceAvailable:boolean}){
const[value,setValue]=useState("");const[languageHint,setLanguageHint]=useState("auto");const[recording,setRecording]=useState(false);const[processing,setProcessing]=useState(false);const[organizing,setOrganizing]=useState(false);const[seconds,setSeconds]=useState(0);const[message,setMessage]=useState("");const[suggestion,setSuggestion]=useState<Suggestion|null>(null);const[browserFallbackAvailable,setBrowserFallbackAvailable]=useState(false);const textareaRef=useRef<HTMLTextAreaElement|null>(null);const recorderRef=useRef<MediaRecorder|null>(null);const recognitionRef=useRef<BrowserSpeechRecognition|null>(null);const streamRef=useRef<MediaStream|null>(null);const chunksRef=useRef<Blob[]>([]);const timerRef=useRef<ReturnType<typeof setInterval>|null>(null);
useEffect(()=>{setBrowserFallbackAvailable(Boolean(browserSpeechConstructor()));return()=>{if(timerRef.current)clearInterval(timerRef.current);if(recorderRef.current?.state==="recording")recorderRef.current.stop();recognitionRef.current?.abort();streamRef.current?.getTracks().forEach(t=>t.stop())}},[]);
function appendTranscript(text:string){const cleaned=text.normalize("NFKC").trim();if(!cleaned)return;setValue(c=>`${c}${c.trim()?"\n\n":""}${cleaned}`.slice(0,8000));setSuggestion(null)}
async function transcribe(blob:Blob){setProcessing(true);setMessage("جارٍ تحويل كلامك إلى نص...");try{const form=new FormData();const type=blob.type||"audio/webm";form.set("audio",new File([blob],`business-note.${extensionFor(type)}`,{type}));form.set("languageHint",languageHint);const response=await fetch("/api/business-notes/voice/transcribe",{method:"POST",body:form,cache:"no-store"});const data=await response.json().catch(()=>({})) as{ok?:boolean;text?:string;code?:string};if(!response.ok||!data.ok||!data.text)throw new Error(data.code||"transcription_failed");appendTranscript(data.text);setMessage("تم تحويل التسجيل إلى نص. راجعه قبل الحفظ.")}catch(e){setMessage(errorText(e instanceof Error?e.message:"transcription_failed"))}finally{setProcessing(false)}}
async function organizeMemo(){const memo=value.normalize("NFKC").trim();if(!memo){setMessage("اكتب أو سجّل تفاصيل المذكرة أولًا ثم اطلب ترتيبها.");return}if(!voiceAvailable){setMessage("التنظيم بالذكاء الاصطناعي غير مفعّل في هذه البيئة بعد.");return}setOrganizing(true);setMessage("INFRO AI ينظم المذكرة الآن...");try{const response=await fetch("/api/business-notes/organize",{method:"POST",cache:"no-store",headers:{"Content-Type":"application/json"},body:JSON.stringify({memo,languageHint})});const data=await response.json().catch(()=>({})) as{ok?:boolean;suggestion?:Suggestion;code?:string};if(!response.ok||!data.ok||!data.suggestion)throw new Error(data.code||"ai_failed");const n=data.suggestion;setSuggestion(n);const form=textareaRef.current?.form??null;setFormValue(form,"title",n.title);setFormValue(form,"noteType",n.noteType,true);setFormValue(form,"summary",n.summary);setFormValue(form,"outcome",n.outcome);setFormValue(form,"nextAction",n.nextAction);setFormValue(form,"stakeholder",n.stakeholder);setFormValue(form,"responsiblePerson",n.responsiblePerson);setFormValue(form,"priority",n.priority,true);setFormValue(form,"workHealth",n.workHealth,true);setFormValue(form,"businessDueAt",n.businessDueDate);setMessage("تم اقتراح تنظيم المذكرة. راجع الحقول قبل الحفظ.")}catch(e){setMessage(errorText(e instanceof Error?e.message:"ai_failed"))}finally{setOrganizing(false)}}
function startBrowserFallback(){const C=browserSpeechConstructor();if(!C){setMessage("تعذر تحويل التسجيل إلى نص تلقائيًا في هذه البيئة. يمكنك الكتابة يدويًا.");return}try{const r=new C();r.lang=browserLanguage(languageHint);r.continuous=true;r.interimResults=false;r.maxAlternatives=1;recognitionRef.current=r;r.onresult=e=>{let text="";for(let i=e.resultIndex;i<e.results.length;i++){const x=e.results[i];if(x?.isFinal)text+=`${x[0]?.transcript??""} `}appendTranscript(text)};r.onerror=e=>setMessage(e.error==="not-allowed"||e.error==="service-not-allowed"?"لم يسمح المتصفح بخدمة التحويل الصوتي. يمكنك الاستمرار بالكتابة يدويًا.":"تعذر التحويل الصوتي الاحتياطي.");r.onend=()=>{recognitionRef.current=null;setRecording(false);if(timerRef.current)clearInterval(timerRef.current);timerRef.current=null};r.start();setSeconds(0);setRecording(true);setMessage("يتم الاستماع الآن عبر التحويل الصوتي الاحتياطي.");timerRef.current=setInterval(()=>setSeconds(c=>{const n=c+1;if(n>=MAX_SECONDS)r.stop();return n}),1000)}catch{setMessage("تعذر بدء التحويل الصوتي الاحتياطي.")}}
async function startRecording(){setMessage("");if(!navigator.mediaDevices?.getUserMedia||typeof MediaRecorder==="undefined"){if(browserFallbackAvailable)startBrowserFallback();else setMessage("هذا المتصفح لا يدعم التسجيل الصوتي المطلوب.");return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true}});streamRef.current=stream;chunksRef.current=[];const mimeType=recorderMimeType();const recorder=mimeType?new MediaRecorder(stream,{mimeType}):new MediaRecorder(stream);recorderRef.current=recorder;recorder.ondataavailable=e=>{if(e.data.size>0)chunksRef.current.push(e.data)};recorder.onstop=()=>{const blob=new Blob(chunksRef.current,{type:recorder.mimeType||"audio/webm"});stream.getTracks().forEach(t=>t.stop());streamRef.current=null;setRecording(false);if(timerRef.current)clearInterval(timerRef.current);timerRef.current=null;if(blob.size>0){if(voiceAvailable)void transcribe(blob);else if(browserFallbackAvailable){setMessage("تم التسجيل. خدمة التحويل الذكي غير مفعّلة هنا؛ يمكنك استخدام التحويل الصوتي الاحتياطي أو الكتابة يدويًا.")}else setMessage("تم التسجيل بنجاح، لكن خدمة التحويل إلى نص غير مفعّلة في هذه البيئة.")}};recorder.start(1000);setSeconds(0);setRecording(true);setMessage("يتم التسجيل الآن. تحدّث بطبيعتك؛ اضغط إيقاف عند الانتهاء.");timerRef.current=setInterval(()=>setSeconds(c=>{const n=c+1;if(n>=MAX_SECONDS&&recorder.state==="recording")recorder.stop();return n}),1000)}catch{setMessage("تعذر الوصول إلى الميكروفون من هذه الصفحة. تحقق من إذن الموقع ثم أعد المحاولة.")}}
function stopRecording(){if(recorderRef.current?.state==="recording")recorderRef.current.stop();if(recognitionRef.current)recognitionRef.current.stop()}
const minutes=Math.floor(seconds/60).toString().padStart(2,"0"),secs=(seconds%60).toString().padStart(2,"0");
return <div className="rounded-2xl border border-slate-200 bg-white"><textarea ref={textareaRef} name="body" required maxLength={8000} rows={6} value={value} onChange={e=>{setValue(e.target.value);setSuggestion(null)}} placeholder="اكتب التفاصيل أو اضغط الميكروفون وتحدث بطبيعتك..." className="w-full resize-y rounded-t-2xl border-0 px-4 py-3 text-sm leading-7 outline-none focus:ring-0"/><div className="flex flex-col gap-3 border-t border-slate-100 bg-slate-50/70 p-3 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-wrap items-center gap-2">{!recording?<button type="button" onClick={startRecording} disabled={processing||organizing} className="inline-flex min-h-10 items-center gap-2 rounded-xl bg-[#07181b] px-4 text-xs font-black text-white disabled:opacity-50"><Mic className="h-4 w-4"/>تسجيل مذكرة صوتية</button>:<button type="button" onClick={stopRecording} className="inline-flex min-h-10 items-center gap-2 rounded-xl bg-rose-600 px-4 text-xs font-black text-white"><Square className="h-4 w-4"/>إيقاف {minutes}:{secs}</button>}<button type="button" onClick={organizeMemo} disabled={recording||processing||organizing||!voiceAvailable||!value.trim()} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-[#9ee9df] bg-[#eafffb] px-4 text-xs font-black text-[#006f69] disabled:opacity-50"><BrainCircuit className="h-4 w-4"/>{organizing?"يتم ترتيبها...":"رتّبها بالذكاء الاصطناعي"}</button><label className="flex min-h-10 items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 text-xs font-bold text-slate-600"><span>لغة الكلام</span><select value={languageHint} onChange={e=>setLanguageHint(e.target.value)} disabled={recording||processing||organizing} className="bg-transparent font-bold outline-none">{LANGUAGE_OPTIONS.map(([o,l])=><option key={o} value={o}>{l}</option>)}</select></label>{processing?<span className="inline-flex items-center gap-2 text-xs font-bold text-[#007f76]"><WandSparkles className="h-4 w-4 animate-pulse"/>جارٍ التحويل...</span>:null}</div><span className="text-[11px] leading-5 text-slate-500">حتى 5 دقائق · الصوت مؤقت · النص لا يتغير دون مراجعتك</span></div><div className="grid gap-3 border-t border-slate-100 bg-white p-3 sm:grid-cols-3"><label className="space-y-1 text-[11px] font-black text-slate-600"><span className="flex items-center gap-1"><BrainCircuit className="h-3.5 w-3.5"/>حالة التنفيذ</span><select name="workHealth" defaultValue="on_track" className="min-h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm font-bold"><option value="on_track">على المسار</option><option value="at_risk">معرّض للخطر</option><option value="blocked">متعثر</option></select></label><label className="space-y-1 text-[11px] font-black text-slate-600"><span className="flex items-center gap-1"><UserRound className="h-3.5 w-3.5"/>مسؤول التنفيذ</span><input name="responsiblePerson" maxLength={160} placeholder="اسم الشخص أو الفريق" className="min-h-10 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label><label className="space-y-1 text-[11px] font-black text-slate-600"><span className="flex items-center gap-1"><CalendarDays className="h-3.5 w-3.5"/>موعد العمل النهائي</span><input type="date" name="businessDueAt" className="min-h-10 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label></div>{suggestion?.reminderSuggested?<div className="border-t border-[#ccefe9] bg-[#f3fcfa] px-4 py-3 text-xs leading-6 text-[#155e59]"><b>اقتراح INFRO: هذه المذكرة تستحق تذكيرًا.</b>{suggestion.reminderReason?<p>{suggestion.reminderReason}</p>:null}</div>:null}{message?<p role="status" aria-live="polite" className="border-t border-slate-100 px-4 py-3 text-xs font-bold leading-6 text-slate-600">{message}</p>:null}</div>;
}
